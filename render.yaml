services:
  - type: web
    name: pc-pick
    runtime: python
    plan: free
    region: singapore
    buildCommand: pip install -r backend/requirements.txt && python db/seed.py
    startCommand: uvicorn backend.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
